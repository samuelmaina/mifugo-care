import Styled from 'styled-components';

export const Customfileupload = Styled.label`
    border: 1px solid #ccc;
	display: inline-block;
	padding: 0.2rem 2rem;
	cursor: pointer;
`;

export const CustomUploaderdet = Styled.strong`
    position: relative;
	top: -10px;
	left: 0.5rem;
`;
